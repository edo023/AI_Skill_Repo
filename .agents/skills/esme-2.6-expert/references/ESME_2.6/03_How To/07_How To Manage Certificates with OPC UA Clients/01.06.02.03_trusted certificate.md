# trusted certificate

trusted certificate

A certificate that is trusted by the relying party based on secured and authenticated delivery. The public keys included in trusted certificates are used to start certification paths. Also known as a "trust anchor".