# Watchdog Timer Output

Watchdog Timer Output

The Rack iPC contains a watchdog timer that resets the CPU. This feature means the Rack iPC Performance recovers from a software failure detection or an EMI issue. The JWDT1 jumper settings control the outcome of what the computer does in the event the watchdog timer is tripped.

The table describes the ME update:

| Function | Jumper setting |
| --- | --- |
| Reset (default setting) | G-SE-0029173.1.gif-high.gif |
| DC | G-SE-0029174.1.gif-high.gif |