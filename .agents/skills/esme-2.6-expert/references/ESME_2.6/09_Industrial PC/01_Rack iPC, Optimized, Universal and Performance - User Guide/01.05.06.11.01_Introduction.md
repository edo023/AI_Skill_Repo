# Introduction

Introduction

Smart Fan Control automatically adjusts the fan speed so that they run faster when the CPU is hotter to maintain the CPU at a constant temperature without running the fan constantly.

This normally involves setting a minimum and maximum fan speed, as well as a high and low CPU temperature. At the low temperature, the fans start running at the minimum fan speed. The fan speed varies in line with the CPU temperature until it reaches the high CPU temperature.