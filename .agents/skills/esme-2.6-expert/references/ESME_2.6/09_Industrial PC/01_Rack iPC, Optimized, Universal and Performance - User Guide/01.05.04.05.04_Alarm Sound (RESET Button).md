# Alarm Sound (RESET Button)

Alarm Sound (RESET Button)

This is an alarm to report the one of the single modules is declined or without the internal signal in redundant mode. It is to sound the alarm until the internal signal returns or push the RESET button.