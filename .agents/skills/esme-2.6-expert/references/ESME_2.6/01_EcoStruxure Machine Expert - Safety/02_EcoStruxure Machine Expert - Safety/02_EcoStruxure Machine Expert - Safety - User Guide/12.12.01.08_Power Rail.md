# Power Rail

Power rails are basic elements of LD networks. Power rails are represented as vertical bold lines delimiting the LD network on both sides.

The state of the left power rail which is necessary for compilation is always TRUE.

The right power rail is optional and has no state.

The green number shown at the top of each left power rail indicates the position of the network in the execution order of the project.

EIO0000002147.09