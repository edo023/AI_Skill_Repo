# Error: Invalid parameter! The input parameter of the function block must be a variable. (Specific code generator)

A function block input is connected to a constant although the input expects a variable.

Double-click the error in the message window to jump to the suspected worksheet (where the error is marked) and replace the constant by a variable or a line.

EIO0000002147.09