# Error: FB instance '...' defined, but never used!

A function block instance is declared in a local variables worksheet. However, this instance is not called in the related code worksheet.

1. In the message window, double-click the error message.

   The variables worksheet opens and the suspected instance declaration is marked.
2. Delete the instance declaration or insert an FB instance call in the code.

EIO0000002147.09