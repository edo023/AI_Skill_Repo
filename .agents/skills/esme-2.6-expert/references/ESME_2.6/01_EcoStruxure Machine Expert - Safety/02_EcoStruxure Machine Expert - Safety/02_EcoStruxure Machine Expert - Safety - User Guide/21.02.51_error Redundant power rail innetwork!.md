# Error: Redundant power rail in network!

Two power rails in a network are connected only by lines without any further contained objects, i.e., these power rails are redundant. This error message is reported for each redundant power rail (at least twice).

Double-click the error in the message window to jump to the suspected worksheet (where the redundant power rail is marked) and delete redundant power rails.

EIO0000002147.09