# Error: The POU '...' is used multiple times, but with a different class property type. If this is a library POU that is used within another library POU, try to recreate the library.

You are using a library that contains nested FB POUs (e.g., library FB A calls library FB B) which was created with a Machine Expert – Safety version before V2.0. Machine Expert – Safety versions before V2.0 did not support such libraries if both library FBs are instantiated in the project. The compiler has been terminated and no valid code has been created.

Recreate such libraries with a Machine Expert – Safety version 2.0 or newer.

EIO0000002147.09