# Error: Code in POU '...' exceeds 64K limit! Shrink the POU.

The code of POU specified in the error message could not be compiled on the Safety Logic Controller because the module code size exceeds 64 KB.

Reduce the size of the specified POU or split it into two or more POUs.

EIO0000002147.09