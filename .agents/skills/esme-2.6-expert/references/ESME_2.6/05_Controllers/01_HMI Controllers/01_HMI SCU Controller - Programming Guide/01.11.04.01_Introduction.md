# Introduction

Introduction

This tab allows you to manage the connection from the PC to the controller:

oHelping you find a controller in a network.

oPresenting the list of controllers, so you can connect to the selected controller and manage the application inside the controller.

oHelping you physically identify the controller from the device editor.

oHelping you change the communication settings of the controller.