# 8975 Motor commutation invalid

Diagnostic class: 2

| Device reactions | Default reaction | Minimum reaction |
| --- | --- | --- |
| Drive | D | E |
| Power supply | - | - |

There is no commutation saved in the motor nameplate. The diagnostic message is only triggered if you try to bring the motor in control mode.

The commutation saved in the motor is only valid if the parameters EncoderGearIn and EncoderGearOut are set to `1`. Otherwise, the motor has to be commutated after each restart (reset or supply voltage on).

The motor commutation has to be determined with the automatic commutation determination. This functionality can be controlled with the parameter MotorCommutationControl.

EIO0000005527.01