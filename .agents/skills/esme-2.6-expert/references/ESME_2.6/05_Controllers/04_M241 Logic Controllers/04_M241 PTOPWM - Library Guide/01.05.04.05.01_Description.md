# Description

## Overview

This function causes the specified axis to move towards a given position at the specified speed, and transfers the axis to the state **Discrete**. To use the MC\_MoveAbsolute\_PTO function block, you must first home the axis. If not the function block will terminate in error (`Error` set to 1 and `ErrorId` set to `InvalidAbsolute`).

EIO0000003077.02