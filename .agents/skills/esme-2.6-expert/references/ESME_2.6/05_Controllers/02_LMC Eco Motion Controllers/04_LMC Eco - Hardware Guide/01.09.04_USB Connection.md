# USB Connection

## Overview

It is possible to connect storage media for the extension of the memory to the connection **CN2** (USB-A). Only a storage medium and no USB-hubs may be connected to the connection

NOTE: The storage medium must only be inserted if the firmware controller is started up. Otherwise the firmware of the controller does not start up.

EIO0000001501.10