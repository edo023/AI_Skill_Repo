# Error: Memory error! POU '...' exceeds 64K module size! Shrink the POU.

The code of specified POU could not be compiled on the Safety Logic Controller because the module code size exceeds 64 KB.

Reduce the size of the affected POU or split it into two or more POUs.

EIO0000002147.09