# Error: FB instance '...' not defined!

A function block instance is used in the code without declaring an instance in the related local variables worksheet.

1. In the message window, double-click the error message.

   The code worksheet opens and the error is marked.
2. Declare the missing FB instance in the variables grid or replace the instance name in the code by an already declared FB instance.

EIO0000002147.09