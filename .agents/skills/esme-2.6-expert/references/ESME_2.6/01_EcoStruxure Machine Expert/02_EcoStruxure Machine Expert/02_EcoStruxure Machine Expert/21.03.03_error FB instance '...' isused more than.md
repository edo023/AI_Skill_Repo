# Error: FB instance '...' is used more than once!

The same function block instance is called more than once in the code (instance name is used several times).

Reading/writing of the same instance data by more than one FB instance is not allowed.

Double-click the error in the message window to jump to the suspected worksheet (where the second instance is marked) and correct the code.

EIO0000002147.09