# Error: The nested comment was not correctly closed. '...' expected.

In an ST code worksheet, the lexical analysis detected that a closing element is expected in a construct of nested comments.

In ST, the following comment formats are allowed

(\* comment \*)

The affected code position and all subsequent errors are underlined in the worksheet.

EIO0000002147.09