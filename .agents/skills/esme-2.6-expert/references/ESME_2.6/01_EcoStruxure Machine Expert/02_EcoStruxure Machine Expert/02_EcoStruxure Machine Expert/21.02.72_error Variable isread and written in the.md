# Error: Variable is read and written in the same network!

A variable is read and written in the same network. This is not allowed.

Error example:

![](images/VariableIsReadWrittenInSameNetwork.gif)

Double-click the error in the message window to jump to the suspected worksheet (where the variable concerned is marked). Use a local variable to split the network.

EIO0000002147.09