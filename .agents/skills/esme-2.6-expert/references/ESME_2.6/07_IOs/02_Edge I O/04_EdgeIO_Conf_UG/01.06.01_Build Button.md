# Build Button

This button allows you to build and verify the configuration.

Upon build completion, a window opens to indicate whether the configuration build is successful or [if errors or advisories are detected](ErrorsAndWarnings-F5630E9C.html "This chapter provides the list of advisory codes and error codes.").

NOTE: Advisories do not prevent the download of the configuration to the network interface module.

EIO0000004810.01