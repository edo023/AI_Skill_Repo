# Functional Description

Functional Description

Displays the encoder position with a delay (represented by the parameter [PositionDelay](EncoderOutput-12.htm#XREF_D_SE_0073772_1)). The value range runs from 0 to [FeedConstant](EncoderOutput-8.htm#XREF_D_SE_0069666_1). With negative direction of rotation, clockwise rotation causes the value range to run in decreasing order.

At system startup, the encoder position is set to 0. If the incremental encoder is connected to a bus terminal, the Position is not 0 after the controller has been reset. 0 is only displayed if the controller is also reset.

For TM5 encoder: The parameter [PositionDelay](EncoderOutput-12.htm#XREF_D_SE_0073772_1) indicates how recent was the last read of the position. (There is a delay because the encoder position is transferred from the module via the TM5 bus and the Sercos back to the controller.)

Use the parameter [State](EncoderOutput-7.htm#XREF_D_SE_0069665_1) to verify the availability or validity of the encoder position data.