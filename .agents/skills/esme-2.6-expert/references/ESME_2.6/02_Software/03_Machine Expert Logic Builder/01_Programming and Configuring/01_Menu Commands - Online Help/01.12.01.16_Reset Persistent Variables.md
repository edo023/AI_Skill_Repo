# Reset Persistent Variables

## Overview

The Online > Reset Persistent Variables command is available in online mode. It resets the persistent variables to their initialization values. If no initialization values are available in the project, the persistent variables are reset to their default values (according to their data type).

EIO0000002860.10