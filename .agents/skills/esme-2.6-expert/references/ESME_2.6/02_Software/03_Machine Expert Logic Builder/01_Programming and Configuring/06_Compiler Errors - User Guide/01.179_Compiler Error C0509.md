# Compiler Error C0509

## Message

Multiple assignments for the operator NEW not allowed

## Message Cause

In one line of code, the assignment operator := is called multiple times with the \_NEW operator.

## Solution

Program the memory allocation with the \_NEW operator in a separate line of code for each pointer that points to dynamically allocated memory.

## Error Example

```
PROGRAM PLC_PRG
VAR
  pbAlpha : POINTER TO BYTE; // Typed pointer to Alpha
  pbBeta : POINTER TO BYTE; // Typed pointer to Beta
  xInit : BOOL := TRUE;
  xDelete : BOOL;
END_VAR
```

```
IF (xInit) THEN
  pbBeta := pbAlpha := __NEW(BYTE); // Incorrect code for memory allocation
END_IF
```

```
pbBeta^ := pbAlpha^ := 16#01;
```

```
IF (xDelete) THEN
  __DELETE(pbAlpha); // Frees memory of pointer
END_IF
```

--> C0509: Multiple assignments for operator \_NEW not allowed

## Error Correction

```
IF (xInit) THEN
  pbAlpha := __NEW(BYTE); // Allocates memory for Alpha
  pbBeta := __NEW(BYTE); // Allocates memory for Beta
END_IF
```

```
pbBeta^ := pbAlpha^ := 16#01; // Multiple assignment
```

```
IF (xDelete) THEN
  __DELETE(pbAlpha); // Frees memory of pointer
  __DELETE(pbBeta); // Frees memory of pointer
END_IF
```

EIO0000003933.04