# Debugging

## Overview

The Tools > Options > Debugging dialog box allows you to determine whether breakpoints are still enabled after a reset.

* If Restore Breakpoints After Reset is selected, the set breakpoints are still enabled after a Reset Cold or a Reset Warm command.
* If Restore Breakpoints After Reset is cleared, the set breakpoints are disabled.

EIO0000002860.10