# Insert Box Parallel Below

## Overview

The FBD/LD/IL > Insert Box Parallel Below command inserts an empty box parallel below the selected function block. It is available if a box is selected in the LD editor.

EIO0000002860.10