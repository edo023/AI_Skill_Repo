# Go To Instance

## Overview

You can execute the Edit > Browse > Go To Instance command in online mode when a POU is open in the editor and the cursor is positioned on an instance of a function block to open the instance of the function block in a new window.

The command is not available for temporary instances or instances from compiled libraries.

EIO0000002860.10