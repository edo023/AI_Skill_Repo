# Compiler Warning C0573

## Message

The ABSTRACT keyword is missing.

## Message Cause

For a method to which the `'abstract'` attribute was set when it was added, the `ABSTRACT` keyword has been subsequently removed in the editor.

## Solution

Insert the `ABSTRACT` keyword back in the editor or remove the `'abstract'` attribute.

EIO0000003933.04