# IScriptList

class

IScriptList.

IScriptList

Bases: object

Represents a collection of objects that can be individually accessed by index.

Parameters

T -- The type of elements in the list.

Type

T

index\_of

item

Determines the index of a specific item in the IScriptList{T} .

Parameters

item (T) -- The object to locate in the IScriptList{T} .

Returns

The index of item if found in the list; otherwise, -1.

Return type

int

**Note**

If an object occurs multiple times in the list, the IndexOf method returns the first instance found.

insert

index

item

Inserts an item to the IScriptList{T} at the specified index.

Parameters

* index (int) -- The zero-based index where the item is to be inserted.
* item (T) -- The object to insert into the IScriptList{T} .

remove\_at

index

Removes the IScriptList{T} item at the specified index.

Parameters

index (int) -- The zero-based index of the item to remove.

add\_range

collection

Adds the elements of the specified collection to the end of the IScriptCollection{T} .

Parameters

collection (IEnumerable<T>) -- The collection whose elements are to be added to the end of the IScriptCollection{T} .

Reports

* NotSupportedException -- The IScriptCollection{T} is read-only.
* ArgumentNullException -- collection is null.

remove\_range

collection

Removes a range of elements from the IScriptCollection{T} .

Parameters

collection (IEnumerable<T>) -- The collection whose elements are to be removed from the IScriptCollection{T} .

Reports

* NotSupportedException -- The IScriptCollection{T} is read-only.
* ArgumentNullException -- collection is null.

EIO0000003799.05