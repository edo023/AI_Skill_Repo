# Insert Device

## Overview

The Insert Device command is used to insert a device object, representing a hardware module, on the same level and above the currently selected object in the Devices tree.

In contrast to the Add Device... command, a device object can be inserted indented below the currently selected object.

The command opens the Add Device dialog box, where you can choose one of devices available for the current position.

EIO0000002860.10