# Aliases

## Overview

|  |  |
| --- | --- |
| Type: | Alias (DUT) |
| Available as of: | V1.0.0.0 |
| Inherits from: | - |

## Description

An alias represents a complex data type which is used in this library.

## Aliases

| Name | Data type | Description |
| --- | --- | --- |
| ArrayReactionLevelForwarding | ARRAY [0...GPL.Gc\_uiMaxLevelForSeverityAndReaction] OF UINT | Mapping table to define reaction level to forward.  The index of the array element corresponds to the reaction level that is active in the diagnostic handler and the value of the array element defines the reaction level that is forwarded to the child. |
| ArraySeverityForwarding | ARRAY [0...GPL.Gc\_uiMaxLevelForSeverityAndReaction] OF UINT | Mapping table to define severity to forward.  The index of the array element corresponds to the severity that is set for the incoming diagnostic message and the value of the array element defines the severity that is forwarded to the parent. |
| ArraySeverityToReactionLevel | ARRAY [0...GPL.Gc\_uiMaxLevelForSeverityAndReaction] OF UINT | Mapping table to define which severity triggers which reaction level.  The index of the array element corresponds to the severity level that is active in the diagnostic handler and the value of the array element defines the severity level that is set. |

EIO0000005555.01