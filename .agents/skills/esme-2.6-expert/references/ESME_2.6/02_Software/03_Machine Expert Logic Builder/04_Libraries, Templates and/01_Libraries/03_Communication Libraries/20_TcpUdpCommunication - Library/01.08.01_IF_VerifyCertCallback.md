# IF\_VerifyCertCallback

## Overview

The IF\_VerifyCertCallback interface is used to retrieve detailed diagnostic information of the certificate verification. It allows you to override the certificate verification while an encrypted TCP connection is being established.

To use the callback functionality you must implement the IF\_VerifyCertCallback interface in your function block.

EIO0000002803.07