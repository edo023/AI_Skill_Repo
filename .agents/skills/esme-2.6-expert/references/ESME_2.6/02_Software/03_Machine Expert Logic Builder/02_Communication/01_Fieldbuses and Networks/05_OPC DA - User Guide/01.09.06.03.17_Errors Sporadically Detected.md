# Errors Sporadically Detected

![](images/logo.png)

Errors Sporadically Detected

Overview

The following errors that are sporadically detected are described in this chapter:

o[Writing values failed to <controller>, GatewayError=28 (affects gateway V2.3)](#XREF_D_SE_0064277_3)

o[Define Symbol List failed. GatewayError=31 (affects gateway V2.3)](#XREF_D_SE_0064277_4)

o[Define VarList failed. GatewayError=31 (affects gateway V2.3)](#XREF_D_SE_0064277_5)

o[Writing values failed to <controller>, Write-List overflow, GatewayError=32! (affects gateway V2.3)](#XREF_D_SE_0064277_6)

o[Sync writing values failed to <Name>, Timeout expired! affects gateway V2.3)](#XREF_D_SE_0064277_7)

o[ProjectID mismatch of symbols and program on <controller>, Project <Project.pro>: PLC=1234567, SYM=2345678 (affects PacDrive M/ gateway V2.3)](#XREF_D_SE_0064277_8)

o[COMM\_FATALERROR[2] from <controller> (affects PacDrive M/ gateway V2.3)](#XREF_D_SE_0064277_9)

o[PLC <controller> is not connected properly (affects PacDrive M/ gateway V2.3)](#XREF_D_SE_0064277_10)

Issue: Message Writing values failed to <controller>, GatewayError=28 (affects gateway V2.3) is Displayed

Cause 1: One controller that is part of the multi-controller configuration of the OPC server has been powered off. It is written to another controller.

The OPC server constantly tries to communicate with the unconnected controllers in defined intervals (also refer to the Reconnect time (s) [parameter](../OPC_Server_Configuration/OPC_Server_Configuration-2.htm#XREF_D_SE_0083829_12)). These attempts usually take 5...6 seconds.

A writing process is also delayed during this time.

Solution 1: Increase the value of the Reconnect time (s) [parameter](../OPC_Server_Configuration/OPC_Server_Configuration-2.htm#XREF_D_SE_0083829_12) to, for example 10,000 ms.

Issue: Message Define Symbol List failed. GatewayError=31 (affects gateway V2.3) is Displayed

Cause 1: This error message is displayed in the OPCServer.log if a single variable (or item) is greater than the buffer size (byte) set in the OPC configuration tool. Thus, the quality of all items is classified as BAD. The maximum buffer size for the PacDrive M controller, for example, is 1500 byte.

Solution 1: Verify the setting of the buffer size in the OPC configuration tool. Verify whether a single variable (for example, an array) is too large.

Issue: Message Define VarList failed. GatewayError=31 (affects gateway V2.3) is Displayed

Cause 1: This error message is displayed in the OPCServer.log if a variable array is too large. The maximum buffer size for the PacDrive M controller, for example, is 1500 byte.

Solution 1: Verify the setting of the buffer size in the OPC configuration tool. Verify whether a single variable (for example, an array) is too large.

Issue: Message Writing values failed to <controller>, Write-List overflow, GatewayError=32! (affects gateway V2.3) is Displayed

Cause 1: This error message is displayed in the OPCServer.log if a single variable (or item) is greater than the buffer size (byte) set in the OPC configuration tool. Thus, the quality of all items is classified as BAD. The maximum buffer size for the PacDrive M controller, for example, is 1500 byte.

Solution 1: Verify the setting of the buffer size in the OPC configuration tool. Verify whether a single variable (for example, an array) is too large

NOTE: If, for example, a list with 100 REAL values is written, 15 bytes are required per item (in newer gateway versions, only 14 bytes) for the write service. 100 x 15 bytes = 1500 bytes (+ additional bytes for service, number of data, ...). Thus, the communication buffer is exceeded.

Issue: Message Sync writing values failed to <Name>, Timeout expired! affects gateway V2.3) is Displayed

Cause 1: This error message is displayed in the OPCServer.log if the timeout has expired.

Solution 1: Increase the value of the Timeout parameter in the OPC server configuration.

Issue: Message ProjectID mismatch of symbols and program on <controller>, Project <Project.pro>: PLC=1234567, SYM=2345678 (affects PacDrive M/ gateway V2.3) is Displayed

Cause 1: This error message is displayed in the OPCServer.log if the project on the controller and the symbol file do not match. The symbol file is out-dated.

Solution 1: Regenerate the symbol file. If the symbol file of the gateway server is loaded from the controller, also transfer the symbol file to the controller.

Issue: Message COMM\_FATALERROR[2] from <controller> (affects PacDrive M/ gateway V2.3) is Displayed

Cause 1: This error message is displayed in the OPCServer.log if the communication has been interrupted.

Solution 1: Verify the communication line (for example, by executing a ping) command.

Cause 2: The timeout for the configured in the OPC configuration tool has expired.

Solution 2: Verify the value configured for the Timeout (ms) [parameter](../OPC_Server_Configuration/OPC_Server_Configuration-2.htm#XREF_D_SE_0083829_12).

Issue: Message PLC <controller> is not connected properly (affects PacDrive M/ gateway V2.3) is Displayed

Cause 1: This error message is displayed in the OPCServer.log if the controller is not correctly connected. The symbol file is available, but no symbols are available in the file.

Solution 1:

Consult the gateway server inspector.

| Step | Action |
| --- | --- |
| 1 | Verify the symbol file configuration in Logic Builder. |
| 2 | Regenerate the symbol file and transfer the project to the PacDrive controller.  In order to regenerate the symbol file in Logic Builder, it may be required to execute the following commands:  oProject > Clear all  oProject > Build all |

EIO0000003723.01

© 2021 Schneider Electric. All rights reserved.