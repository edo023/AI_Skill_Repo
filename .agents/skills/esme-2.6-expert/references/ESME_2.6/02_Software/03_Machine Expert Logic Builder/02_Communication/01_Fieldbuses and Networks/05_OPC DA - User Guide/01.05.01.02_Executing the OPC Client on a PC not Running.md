# Executing the OPC Client on a PC not Running EcoStruxure Machine Expert

Executing the OPC Client on a PC not Running EcoStruxure Machine Expert

To be able to execute the OPC client on a PC where EcoStruxure Machine Expert is not installed, proceed as follows:

oInstall the gateway component on the PC where the OPC client is running. Depending on the OPC client, you need to launch the WinCoDeSysOPC.exe file.

oCopy the file OPCServer.ini to the same directory where the WinCoDeSysOPC.exe is installed.