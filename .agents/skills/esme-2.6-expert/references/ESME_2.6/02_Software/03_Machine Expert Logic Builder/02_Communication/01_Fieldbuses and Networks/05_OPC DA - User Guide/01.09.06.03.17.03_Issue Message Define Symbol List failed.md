# Issue: Message Define Symbol List failed. GatewayError-31 (affects gateway V2.3) is Displayed

Issue: Message Define Symbol List failed. GatewayError=31 (affects gateway V2.3) is Displayed

Cause 1: This error message is displayed in the OPCServer.log if a single variable (or item) is greater than the buffer size (byte) set in the OPC configuration tool. Thus, the quality of all items is classified as BAD. The maximum buffer size for the PacDrive M controller, for example, is 1500 byte.

Solution 1: Verify the setting of the buffer size in the OPC configuration tool. Verify whether a single variable (for example, an array) is too large.