# i_xHookModeEn

i\_xHookModeEn

TRUE on this input enables the hook mode. Commands to close drive are ignored in the hook mode. The axes operate either in load sharing or position synchronization mode, based on the state of i\_xTrqShareEn input.

NOTE: Switching between hook mode and standard grab operation while the axes are running is possible but not recommended.