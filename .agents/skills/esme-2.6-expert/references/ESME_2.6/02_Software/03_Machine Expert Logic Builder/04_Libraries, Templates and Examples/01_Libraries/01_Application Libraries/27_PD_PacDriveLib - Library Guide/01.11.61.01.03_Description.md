# Description

Description

The function sets the bit of the DWORD i\_dwInput given in the variable i\_iConversionBit to 0 if i\_xSet = FALSE or to 1 if i\_xSet = TRUE. The bits are enumerated from low to high and fall within the range 0 to 31.