# Overview

## Graphical Representation

![](images/T001605697.gif)

## TeSysU\_HW\_2D Device Module Description

The Device Module provides a ready-to-use coding template as a pattern to monitor and control a hardwired TeSys U (reversible type) through a Schneider Electric controller.

The Device Module TeSysU\_HW\_2D is represented by a function template and consists of a global variable list (GVL), a program. After instantiation of the Device Module, these objects are added to your project. They appear with the name which has been assigned using [**Add Function From Template**](../../../../../api/crossBook?lang=en-US&virtualBookName=SoMProg&topicID=D_SE_0083799).

The GVL provides the variables which are used to monitor and control the TeSys U via hardwired I/Os.

The program provides the following features:

* monitor the state of the device
* control the device in manual mode (latch mode)
* control the device in local mode (latch mode)
* control the device in auto mode (jog mode)

EIO0000002835.04