# Description

Description

This function is used to specify that an axis, when homed, will be homed by setting the position of the axis based on the encoder position and/or stored values. The i\_etMode input specifies which position is changed as follows:

| i\_etMode | Home Action |
| --- | --- |
| PDL.ET\_HomeMode.SetPosAxisPosition | The axis’s position is set to the value at input i\_lrPosition |
| PDL.ET\_HomeMode.SetPosLogEncoderPosition | The logical encoder is set to the value at the i\_lrEncoderPosition input. |
| PDL.ET\_HomeMode.SetPosAxisAndLogEncoderPosition | Performs both of the above functions |

All possible values of i\_etMode are listed in PDL.ET\_HomeMode. The function returns FALSE if none of the above three values is used.