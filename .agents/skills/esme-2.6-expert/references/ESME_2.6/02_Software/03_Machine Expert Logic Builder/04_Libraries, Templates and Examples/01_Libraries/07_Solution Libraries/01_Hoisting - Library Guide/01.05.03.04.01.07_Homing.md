# Homing

Homing

The FB supports various homing methods. It uses positive and negative limit switches or an optional reference position limit switch to define a home position.

For detailed description of homing methods, refer to the description of the i\_stHmngPara structure. Homing is performed automatically (without movement of the axis) after the first download and after a change between linear and modulo mode.