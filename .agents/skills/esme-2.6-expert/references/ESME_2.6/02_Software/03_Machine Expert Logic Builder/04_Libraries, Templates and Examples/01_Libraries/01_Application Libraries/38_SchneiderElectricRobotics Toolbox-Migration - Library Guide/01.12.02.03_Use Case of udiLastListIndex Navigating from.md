# Use Case of udiLastListIndex: Navigating from the Last to the First Element in the List

## Overview

This example illustrates how to navigate from the last to the first element in the list:

| Illustration | Code Example |
| --- | --- |
|  | ``` //start from the last element of the list udiIndex := ifTargetsHandler.udiLastListIndex;  WHILE udiIndex <> Gc_udiHeadListIndex DO     //get the data on the target at the current index     stTempTarget := ifTargetsHandler.GetTarget(udiIndex);      //do something      //get the PREVIOUS index     udiIndex := ifTargetsHandler.GetPreviousListIndex(udiIndex); END_WHILE ``` |

EIO0000006044.00