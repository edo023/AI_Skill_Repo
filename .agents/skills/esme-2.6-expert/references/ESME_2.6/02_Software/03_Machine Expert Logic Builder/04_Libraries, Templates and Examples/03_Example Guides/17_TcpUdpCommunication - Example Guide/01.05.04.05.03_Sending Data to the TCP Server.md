# Sending Data to the TCP Server

Sending Data to the TCP Server

After the TCP client has established a connection to the TCP server, [a data packet](Project_Information-6.htm#XREF_D_SE_0088484_3) is sent to the server. You can modify the text message in the visualization. The TCP server implemented in this example application only accepts the message GetTimestamp.