# Preconditions for Running the Application Example

## Overview

To run the application example, the following preconditions must be fulfilled:

* The application is downloaded and running on the controller
* An email server with the appropriate configuration and the required accounts is reachable from the controller via the Ethernet network.
* Another client is connected to the email server to exchange emails with the controller.

EIO0000002821.03