# Overview

Overview

To run the application example, the following preconditions must be fulfilled:

oEach application was downloaded and is running on the associated controller.

oA PC, laptop, or mobile device with a compatible web browser is connected to the same network as the controllers so that the web visualization can be accessed.