# Unit Scaling

Unit Scaling

Unit scaling allows scaling of the internal position value to user units. The absolute position of an axis is internally stored as a 32 bit integer value. For detailed description of unit scaling, refer to the description of the i\_stEncPara structure.