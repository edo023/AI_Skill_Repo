# Limitations

Limitations

oThere is no provision for giving speed reference value from inside the block. It has to be set from outside the block.

oIf both forward and reverse run command are given at the same time then none of them will take effect on the output side, i.e there will be no run order in any direction.

oIf both forward run and stop command are given at the same time then stop has higher priority than forward run.

oIf both reverse run and stop command are given at the same time then stop has higher priority than reverse run.