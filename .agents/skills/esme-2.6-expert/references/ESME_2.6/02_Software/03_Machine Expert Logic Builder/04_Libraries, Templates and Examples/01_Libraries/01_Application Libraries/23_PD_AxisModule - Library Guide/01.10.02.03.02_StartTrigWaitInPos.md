# StartTrigWaitInPos

StartTrigWaitInPos

The input iq\_diCmd shows the positioning command until the positioning is done and the output q\_xInPosition is TRUE. This will simplify the handling of commands in the command table. The table will not continue until the positioning is done.