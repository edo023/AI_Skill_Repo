## Library parameters

Library parameters can be created in each library.

As soon as the library is then referenced in a project, the constant in the parameter list can be adapted according to the requirements.