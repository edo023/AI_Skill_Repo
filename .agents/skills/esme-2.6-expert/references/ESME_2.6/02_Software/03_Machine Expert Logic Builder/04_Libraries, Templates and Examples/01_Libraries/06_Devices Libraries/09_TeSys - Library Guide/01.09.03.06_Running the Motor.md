# Running the Motor

Running the Motor

The motor can run, only if the interlock input i\_xLock is set to 0. An active interlock signal inhibits the start/stop of the motor. When the interlock signal returns to 0 the motor is restarted. An active interlock is indicated at the output q\_xLock. The motor can run only if the output q\_xErr is set to 0.

An active detected error signal inhibits the start or stop of the motor. The function block sets the detected error signal, if the detected error input i\_xErr is set to 1 (external detected error) or in case of an invalid operation mode or a missing feedback signal (internal detected error).