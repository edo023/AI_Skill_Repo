# How to Configure a Connection Between the Controller and the SQL Gateway

## Overview

The SQL Gateway supports secured connections to the controller. If your controller does not support secured communication, you can configure unsecured communication between the controller and the SQL Gateway.

Specific ports are reserved for each communication type. You can configure and use them in parallel as described in the following sections.

EIO0000002417.08